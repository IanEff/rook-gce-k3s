apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Installs experimental Gateway API CRDs (includes GRPCRoute for Hubble relay).
# Update the version tag to match GATEWAY_API_VERSION in gitops.env.
resources:
  - https://github.com/kubernetes-sigs/gateway-api/releases/download/v1.5.1/experimental-install.yaml
