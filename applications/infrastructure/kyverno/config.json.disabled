{
    "_disabled": "2026-07-13: attestation testing (verify-thump-images) is done; this cluster is thump's ephemeral testbed and the policy blocks every Tilt dev-loop pod (unsigned tilt-* image tags). Rename back to config.json to re-enable — all manifests below are untouched.",
    "appName": "kyverno",
    "syncWave": "40",
    "namespace": "kyverno",
    "localPath": "applications/infrastructure/kyverno"
}
